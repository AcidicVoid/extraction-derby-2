"""
Destruction Derby 2 (PS1, 1996) asset extraction toolkit.

Every parser validates its structural invariants and raises on violation, so a
wrong format assumption fails loudly rather than producing plausible garbage.
"""

__version__ = "0.1.0"
