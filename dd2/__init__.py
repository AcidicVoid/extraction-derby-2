"""
dd2 — Destruction Derby 2 (PS1, 1996) asset extraction toolkit.

Nothing in this package trusts prior reverse-engineering work. Every parser
validates its own structural invariants and raises on violation, so a wrong
format assumption fails loudly instead of producing plausible-looking garbage.
"""

__version__ = "0.1.0"
